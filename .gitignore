# Node / pnpm
node_modules/
.pnpm-store/
*.log
npm-debug.log*
yarn-debug.log*
pnpm-debug.log*

# Build output
out/
dist/
release/
*.tsbuildinfo

# Runtime / data — per CLAUDE.md §4, `data/` is gitignored
data/
*.db
*.db-journal

# Editor / OS
.vscode/
.idea/
.claude/
.DS_Store
Thumbs.db

# Env
.env
.env.local
.env.*.local

# Logs
logs/
*.log

# Vendored reference repos. Each subdir is its own git clone; we don't track
# them in version control to avoid bloat and double-git-dir confusion. Director
# / collaborators clone them locally per docs. See PR #1 self-review for the
# reasoning — submodules are an option if drift becomes a real problem.
_ingest/
