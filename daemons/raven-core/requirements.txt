# Aether-voice raven-core runtime deps. Vendored from VIEWER and trimmed:
#   - flask, flask-cors, cerebras-cloud-sdk dropped (only the disabled
#     cerebras_tool / cerebra.py sidecar needed them; cerebra.py was
#     deleted in this PR).
#   - opencv-python, mss, pillow kept even though we always launch with
#     --mode none, because raven_core/vision.py top-level-imports cv2 +
#     mss and raven_core/orchestrator.py top-level-imports vision. A
#     follow-up PR can prune vision when system_tool / camera modes are
#     decided.
#
# aiohttp added when raven became a mesh participant: the vendored Python
# SDK at core/node_sdk imports aiohttp for its HTTP + SSE client. It is
# a transitive dep of Core itself, so most dev machines already have it
# in their global Python — this pins it into raven-core's venv too.
google-genai
opencv-python
pyaudio
pillow
mss
requests
aiohttp
