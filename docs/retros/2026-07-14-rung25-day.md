# 2026-07-14 — the day the loop got eyes, then audited its Architect

**Shipped:** #379 raven gatePhase mirror (R2 Python parity) · #382 §10 field-gotcha batch · #385 deny wire-reason invariant (SDK flip + ADR) · #386 Rung 2.5 lane gate monitor · #387 deny detail restoration · #388 validator description limits. Banked: the north-star operating model — zero PR-less issues, the drain loop, self-merge behind validation maturity, Director feedback post-merge on live main (#224) — plus the A2 shadow-mode criteria skeleton.

**Rung 2.5 inverted the cadence the night it merged.** Every lane-state fact after the merge — at-gate, stalled-at-blocker, the toasts — arrived by notification, zero pane attachments. Its motivating failure (lane-372's eight silent days) is reproduced in its own tests.

**R2 proven in production twice** (374 rehearsal cycle; 371 Director-directed ADR fold-in). Revision cost: minutes. R2 is an organ, not a feature.

**The ghost relay was the Architect.** Two "ghost" revise sentences (07-06 lane-372, 07-15 lane-384) triggered #380 with four evidence addenda pointing at the voice tool and the shell. Task-1 findings exonerated BOTH code paths — structurally (the ledger cannot lose a relay it executes) and behaviorally (transcripts show no revise utterance) — and clipboard forensics named the real rail: Architect-dictated, Director-pasted manual send-keys blocks, un-ledgered, un-allowlisted, un-verified, with paste mechanics that swallow Enter. The §13.9 manual-completion lineage never retired; the tools built to replace it were bypassed by the person who designed them. Law minted: gate-relay sentences ride the executor ONLY; the manual rail is retired; terminal lanes attach-and-type until a ledger-registration path exists.

**Laws minted or refined:**
- The valve counts NON-COMMENT implementation lines (#378 ruling).
- Merge triggers never ship in the same message or block as their verification — the trigger lives behind the round-trip (minted after branch protection caught a premature paste-batch merge; the mechanical gate backstopped the procedural one, by design).
- App-spawned lanes close out THROUGH the app; manual teardown orphans the ledger.
- Both Claude cells red in under 60s with ci green = API credits, not a verdict; the gate failed CLOSED — correct posture.
- The reviewer verdict is the TEXT on the true head, never the check's exit status; rebases invalidate verdicts and stale check displays lie.
- Gate relays ride the executor only (see above) — the day's capstone.

**Also open, evidence-loaded:** #383 fold blindness (raven enumerated one lane where two lived; duplicate-request/no-outcome record shape). #384's blocker → Option A: the Architect's one-node premise falsified by recon (seven nodes over the 280 limit) — §9 discipline exactly as designed.

**The premise ledger: seven Architect premises falsified by implementer evidence across two sessions** — the seventh being the ghost's attribution itself. Specs carry premises; recon audits them; the gate is where truth wins — including against the Architect.

**#227 seeded twice:** a Python CI rail, and a per-node test-scaffold floor.
