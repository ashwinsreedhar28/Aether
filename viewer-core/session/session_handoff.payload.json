{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "https://viewer.local/schemas/session/session_handoff.payload.json",
  "title": "session_handoff payload",
  "description": "Payload for the viewer session `session_handoff` surface. Rehydrates the whole session onto the target device by invoking that node's open_view (and focus_view) over the mesh.",
  "type": "object",
  "required": ["target"],
  "additionalProperties": false,
  "properties": {
    "target": {
      "type": "string",
      "enum": ["desktop", "spatial"],
      "description": "Which viewer node to move the workspace to: desktop=viewer_desktop, spatial=viewer_spatial."
    }
  }
}
