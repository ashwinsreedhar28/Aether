{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "https://viewer.local/schemas/mesh/close_window.payload.json",
  "title": "close_window payload",
  "description": "Payload for the viewer node `close_window` surface. Closes ANY window in the scene by its shell windowId (from list_windows), regardless of who opened it.",
  "type": "object",
  "required": ["windowId"],
  "additionalProperties": false,
  "properties": {
    "windowId": {
      "type": "string",
      "minLength": 1,
      "description": "Shell window id to close (the `id` field returned by list_windows)."
    }
  }
}
