{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "https://viewer.local/schemas/mesh/focus_window.payload.json",
  "title": "focus_window payload",
  "description": "Payload for the viewer node `focus_window` surface. Brings ANY window in the scene to the foreground by its shell windowId (from list_windows), regardless of who opened it.",
  "type": "object",
  "required": ["windowId"],
  "additionalProperties": false,
  "properties": {
    "windowId": {
      "type": "string",
      "minLength": 1,
      "description": "Shell window id to focus (the `id` field returned by list_windows)."
    }
  }
}
