{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "https://viewer.local/schemas/mesh/list_windows.payload.json",
  "title": "list_windows payload",
  "description": "Payload for the viewer node `list_windows` surface. Takes no arguments. Returns the FULL scene — every window in every workspace, regardless of who opened it (user, this node, or another agent) — so the caller has full-scene visibility, not just views it painted.",
  "type": "object",
  "additionalProperties": false,
  "properties": {}
}
